<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    tools:context=".RegistroActivity">

    <ImageView
        android:id="@+id/registerActivityImagenView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:src="@drawable/ic_baseball_bat"
        android:tint="@color/colorWhite" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/registerActivityImagenView"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Register"
            android:textColor="@color/textColorLight"
            android:textSize="20dp"
            android:textStyle="bold" />

        <EditText
            style="@style/RegisterActivityText"
            android:hint=" User Name" />

        <EditText
            style="@style/RegisterActivityText"
            android:hint="Email Address"
            android:inputType="textEmailAddress" />

        <EditText
            style="@style/RegisterActivityText"
            android:hint="Password"
            android:inputType="textPassword" />

        <EditText
            style="@style/RegisterActivityText"
            android:hint="Retype Password"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/registarActivitySignUp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@drawable/gradientbuttonlight"
            android:text="Sign Up"
            android:textAllCaps="false"
            android:textColor="@color/colorPrimary"

            />
    </LinearLayout>

    <TextView
        android:id="@+id/registerActivityLogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:text="Already a member ? log Here" />

</RelativeLayout>